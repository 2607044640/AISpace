---
trigger: always_on
---

# Web Research Policy (Antigravity-only)

- Web lookups: use `read_url_content` or `search_web` ONLY.
- NEVER use `browser_subagent` for research or documentation.
- `browser_subagent` is reserved only for tasks that explicitly require UI interaction.
